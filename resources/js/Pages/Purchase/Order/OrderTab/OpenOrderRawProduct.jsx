import React from 'react';
import PropTypes from 'prop-types';

const OpenOrderRawProduct = props => {

    console.log(props)

    return (
        <div>

        </div>
    );
};

OpenOrderRawProduct.propTypes = {
    data: PropTypes.any
};

export default OpenOrderRawProduct;
